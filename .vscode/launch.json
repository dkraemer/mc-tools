{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "mc-curse-export",
            "request": "launch",
            "command": "yarn run mc-curse-export",
            "cwd": "${workspaceFolder}"
        }
    ]
}
